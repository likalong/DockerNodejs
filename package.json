{
  "name": "my_docker_node",
  "version": "1.0.0",
  "description": "this is for learning build docker on node",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "lika",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
